请帮我在lab/static/static.cpp(.sh)中使用verilator写一个实验代码，分别使用不同规模（MNK）的连续GEMM命令流（单次实验的流内MNK保持不变，但数据没有依赖关系），测量指标是GEMM命令吞吐量和GEMM命令平均完成延迟。初步设定的主测试硬件是1lane 64x64, 4lane 32x32, 16lane 16x16，OPRandBufSize=16，其他参数保持默认SubtileK=32, LoadDataWidth=256，paccnum=16。因此，这个脚本需要接收 Lane Width M N K Count 参数，表示硬件规模、GEMM命令规模和命令总个数。将结果以txt形式写入到运行目录的./data/L{Lane}_W{SA_WIDTH}_{M}X{N}X{K}_Cnt{Count}.txt，两行，throughput=xxx(GEMM cmd per KCycle) latency=xxx(KCycle)，然后运行一些比较小规模的任务进行脚本测试。

然后，请分别使用三个硬件规模运行MNK三个维度分别的对比实验。默认的M为16，NK均为4096，第一组改变M，第二组改变N，第三组改变K。其中，M的取值集合为{1,4,16,32,64,128,256,1024}，N和K的取值集合为{32,64,128,256,512,1024,2048}